import 'package:flutter/material.dart';

class samar extends StatelessWidget {
  const samar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('rishabh')),
    );
  }
}
